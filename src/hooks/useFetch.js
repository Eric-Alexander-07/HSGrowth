// useFetch.js - Hook-Skelett für spätere API-Aufrufe (ohne Logik).
const useFetch = () => {
  // TODO: Anfrage-Handling, Caching und Fehlerzustände hier ergänzen.
  return {
    data: null,
    error: null,
    loading: false,
  }
}

export default useFetch
